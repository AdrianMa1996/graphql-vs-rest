import { CreateContributionDto } from './create-contribution-dto';

describe('CreateContributionDto', () => {
  it('should create an instance', () => {
    expect(new CreateContributionDto()).toBeTruthy();
  });
});
