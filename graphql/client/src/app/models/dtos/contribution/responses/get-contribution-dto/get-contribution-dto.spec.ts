import { GetContributionDto } from './get-contribution-dto';

describe('GetContributionDto', () => {
  it('should create an instance', () => {
    expect(new GetContributionDto()).toBeTruthy();
  });
});
