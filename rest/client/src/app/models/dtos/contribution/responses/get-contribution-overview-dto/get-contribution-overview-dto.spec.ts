import { GetContributionOverviewDto } from './get-contribution-overview-dto';

describe('GetContributionOverviewDto', () => {
  it('should create an instance', () => {
    expect(new GetContributionOverviewDto()).toBeTruthy();
  });
});
