import { UpdateProjectAndUserBindingDto } from './update-project-and-user-binding-dto';

describe('UpdateProjectAndUserBindingDto', () => {
  it('should create an instance', () => {
    expect(new UpdateProjectAndUserBindingDto()).toBeTruthy();
  });
});
